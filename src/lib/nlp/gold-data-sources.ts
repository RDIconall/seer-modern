/**
 * Legacy labeled sentence exports for offline eval (see eval-harness.ts).
 * Paths are relative to the monorepo root when both trees live together.
 */
export const LEGACY_GOLD_SQL_RELATIVE = "seer-master/backup/USER_SENTENCE_RATINGS_11-28-13.sql";
